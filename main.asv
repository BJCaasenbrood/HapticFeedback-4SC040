clear; close all; clc;
%% load default settings
loadParameters;

%% User settings
OPTION = 2;
ke = 500;
Kp = 75;
Kd = 0.12;
%% run model
sim('mainModel');

%% plot data
figure(101);
subplot(2,1,1);
show(t,xd,'k--');
show(t,xm);
show(t,xs);
subplot(2,1,2);
show(t,abs(xm - xs));

figure(102);
subplot(2,1,1);
show(t,Fh);
show(t,Fe);
subplot(2,1,2);
show(t,abs(Fh - Fe));
